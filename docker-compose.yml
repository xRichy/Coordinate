services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: coordinate
      POSTGRES_PASSWORD: coordinate
      POSTGRES_DB: coordinate_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
