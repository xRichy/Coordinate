// tRPC sub-router for crm-pipeline.
// Lead CRUD procedures are implemented in packages/api/src/routers/crm.ts
// under crm.lead.* (same pattern as crm.contact.*).
export const crmPipelineRouter = {} as const;
