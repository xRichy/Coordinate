// The dashboard's aggregate procedures live in packages/api/src/routers/dashboard.ts
// under dashboard.* (stats). No module-local sub-router is needed.
export const dashboardModuleRouter = {} as const;
