// tRPC sub-router for quotes.
// The actual procedures live in packages/api/src/routers/quotes.ts under
// quotes.* (same pattern as warehouse / crm-pipeline).
export const quotesModuleRouter = {} as const;
