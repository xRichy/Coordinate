// Warehouse procedures (product CRUD + stockMovement) are in
// packages/api/src/routers/warehouse.ts under warehouse.product.* and
// warehouse.stockMovement.*.
export const warehouseModuleRouter = {} as const;
