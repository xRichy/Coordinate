// tRPC sub-router for work-orders.
// The actual procedures live in packages/api/src/routers/work-orders.ts under
// workOrders.* (same pattern as quotes / warehouse).
export const workOrdersModuleRouter = {} as const;
